### How to install :

`
python -m venv myenv 
.\myenv\Scripts\activate 
pip install fastapi uvicorn pip
install -r requirements.txt
`

### How to Run :

`
.\myenv\Scripts\activate
uvicorn fastapi_htmx.main:app --reload
`

#### Prower Shell :

`> Kill process:
Get-Process | Where-Object { $_.ProcessName -like "python*" } 
 Stop-Process -Id XXXX -Force`
